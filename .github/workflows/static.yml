name: Disabled static Pages deployment

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is disabled. Use deploy.yml for GitHub Pages deployments."
